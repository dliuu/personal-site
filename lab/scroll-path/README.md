# scroll-path

## Trying

Four boxes, four camera keyframes, a Catmull-Rom path sampled by scroll
progress. Proves ScrollTrack, useScrollProgress (lerp + reduced-motion snap),
useInvalidateOnScroll with frameloop="demand", cameraPath, progressToSection,
and the Overlay DOM panel.

Note: on a demand-mode canvas the Stats FPS counter freezes while idle. That is the expected proof of 'no scroll, no frames', not a bug.

## Result

_(fill in after viewing)_

## Keep / Drop

_(fill in)_
