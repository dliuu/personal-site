# Decisions

One line per decision, newest last. Experiments record their own verdicts in
their README; this file records what changed the project's direction.

## Budgets

- Initial JS (home and /lab): under 600 KB gzipped.
- Any experiment route: under 1 MB gzipped first load.
- No single static asset over 1 MB.
- Check by reading `next build` output until it matters enough to automate.
- 2026-09-18 measured: `next build` on Next 16 prints no size table; measure first-load JS in browser devtools (Network, JS, gzipped) when it matters.

## Log

- 2026-09-18: Skeleton built. Next.js 16 + r3f 9 + drei + leva + zustand + vitest. npm, no Tailwind.
- 2026-09-18: Home page is a placeholder until a direction is chosen. Ideas go in /lab.
- 2026-09-18: The studio-room design (local spec) is demoted to a candidate; lighting-bench decides whether it proceeds.
- 2026-09-18: Vercel deployment protection is on; preview URLs need a logged-in browser. Curl checks see 302 to SSO.
- 2026-09-18: Implementation deviates from docs/skeleton.md §4/§5.2/§6.1 on purpose: registry metadata (`lab/registry.ts`) and dynamic loaders (`lab/loaders.tsx`) are split because `ssr:false` must live in a client module; `useScrollProgress` returns `{ step }` for use inside `useFrame`; `lint` is `eslint .` because Next 16 removed `next lint`.
- 2026-09-19: Direction changed from a 3D room to editorial chapters over one fixed canvas (Shopify Editions model, wiki entry 1). Theme is a halftone post pass carried into type and palette. studio-room stays in the lab (PR #2) as the rejected comparison. Added postprocessing + @react-three/postprocessing.
- 2026-09-18: studio-room experiment added as the candidate direction. Reduced-motion transition kill-switch added to globals.css for all experiments.
- 2026-09-20: instruments experiment added as a Renaissance variant of chapters (instrument heroes, engraving post effect, draw-in, codex DOM). Section store and hook promoted to shared code.
- 2026-09-20: instruments: draw-in scales whole solid/line roots (per-part scaling exploded the assemblies); mech axis is per instrument; engraving AA is box-filtered.
- 2026-09-20: instruments carries the professional profile (five chapters). Astrolabe and gear train retired; balance, globe and bridge added.
- 2026-09-20: Added a headless screenshot loop (Playwright + SwiftShader WebGL) so visual fixes can be made by sight; outputs in shots/ are gitignored.
- 2026-09-20: explore: hero region reserved beside the column (lib/heroRegion.ts); per-chapter palette via canvas uniforms and inline CSS variables; hotspot notes via drei Html; drag-to-turn; .instruments is pointer-transparent again (chapters removed the same scaffolding in dc2fa70 when nothing in the canvas was interactive).
- 2026-09-21: plates: a chapter may own a plate section (`kind: "plate"`, 300vh, linear pinned progress) where its instrument expands to full screen and scroll scrubs beats read from an imperative `plateState` singleton (not the store: it is written and read inside `useFrame`). Only Meta has a plate for now; Eisen and WCP plates are storyboarded in the spec. Plate captions sit in a paper cartouche because the beat-1 close-up puts hatching behind the text.
- 2026-09-21: meta plate colour: the engraving gains a chroma wash under the hatching (a hand-tinted print) driven by plate expand, so chapters stay grey and hatch density still follows light. Geography is Natural Earth 110m baked to public/geo/land.json (land texture + coastline lines); pins sit at 48 placeholder locale cities (owner input). Spot colours per beat: vermilion pins and Manhattan, brass gauge with four red survivors, gold satellites. The globe's yaw through the plate is scripted per beat (`plateYaw`) and idle spin is an accumulated angle, so framing is deterministic and nothing unwinds on exit. Shots gain a 0.82 scrub point for beat iv.
- 2026-09-21: meta plate realistic: in the plate the engraving dissolves (`reveal` uniform blends the effect toward the raw render) and the globe is drawn as a photoreal Earth (three.js example planet textures, physical material, alpha clouds, fresnel atmosphere) on brass that turns metallic with reveal, lit by a procedural drei Environment whose intensity follows reveal, over a gradient-texture contact shadow. A second Earth sphere fades in over the hatched one so the chapter band is unchanged. SMAA and a reveal-gated bloom are high-tier only. Hand-tinted engraving remains one uniform away.
- 2026-09-22: meta plate immersive: the globe is centred at most of the screen and scroll turns it one full time west → east (`lib/plateCamera.ts`: face-yaw keyframes unwrapped so it only turns east, 30 % of each beat travelling, then a slow drift), while the camera orbits the sphere centre to each beat's latitude and zoom. Six beats (one per bullet plus the arrival) with callout cards (drei Html) anchored on the globe: dot, leader, card; anchor-less beats and narrow screens keep the docked caption. Beat i fans 48 tube arcs out of Manhattan; close-ups light the visited region's pins. Plate height is beats × 90vh; the exit ramp moved to 0.94 so the last beat plays in colour. Shots capture one frame per beat from `data-beats`.
- 2026-09-22: intro desk scene: chapter I is a full-screen 3D room (a scene chapter is its own plate, no text column): a placeholder jointed figure types at a desk under a warm lamp and monitor glow, stands and waves on scroll, and the camera goes over the desk into the monitor until the screen fills the viewport; Eisen is the desktop inside it (`screen: true`, drawn raw, dark palette). Pure timeline in lib/introTimeline.ts; camera via lib/cameraPath.ts. Budget exception approved: one hero model up to 3 MB, lazy-loaded (the owner's Ready Player Me avatar with Mixamo clips replaces the placeholder). Shots capture scene plates at 0.02/0.2/0.45/0.75/0.97.

## Budgets (amended 2026-09-22)

- One hero model (the intro avatar) may be up to 3 MB, lazy-loaded after first paint, never in first paint.
- 2026-09-23: intro room immersive: the room is fully dressed (walls, ceiling, door, window bay with instanced blinds and a rain-on-glass shader over a canvas skyline, shelf wall with canvas book spines, record player, cassette deck, photo, neon in the script font, cork board, floor cushion, string lights, a cat), alive (dust in the lamp cone, steam, twinkle, keys that press with the typing, phone pulses, record and fan turn, filament flicker, camera breathing, pointer parallax, head look, one establishing dolly per session) and interactive (lamp, record player/sound, blinds cord, cat, notes, mug, poster; the same toggles as keyboard-reachable buttons in the overlay; a tooltip names what the pointer is over). Zero new assets: every texture is a canvas painter in lib/roomTextures.ts and all sound is Web Audio (lib/roomAudio.ts schedule), built on the first gesture. Detail mounts in two idle slices after first paint; two shadow maps on high, none below; instanced meshes are laid out per frame (a one-off effect lost its matrices). Book titles, notes and the neon phrase are placeholders (owner input).
- 2026-09-23: intro room re-dressed: open, nature-focused and modern in a soft earthy palette (plaster walls, pale oak, linen, sage, terracotta), golden-hour daylight (a sun through the glass with one 1024 shadow map, a sky hemisphere) instead of night; the back wall is three floor-to-ceiling glass panels with a painted garden and instanced trees outside for parallax; linen curtains on a rail replace the blinds (store key `curtainsOpen`); the neon is gone; plants roughly tripled (hanging pothos, plant stand, a bench of pots under the glass, two tall trunked plants, shelf and desk pots). The intro palette is now a warm cream so the overlay reads in ink, in a cartouche over the garden.
